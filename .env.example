# Demo mode works with all values empty. Never commit .env.local.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server-only: needed for deleting the authenticated user's account.
SUPABASE_SERVICE_ROLE_KEY=
# Server-only, OpenAI-compatible Chat Completions endpoint with JSON output.
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=
