-- Browser demo seeds are intentionally device-local and generated by
-- lib/habitpilot/core.ts -> demoState(timezone), relative to today's local date.
-- Real accounts start empty. Do not insert sample data into real users' accounts.
-- Integration-test accounts are created and removed by tests/supabase.integration.mjs.
